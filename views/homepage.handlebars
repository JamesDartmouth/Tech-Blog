<div>

  {{#if posts.length}}
  <div class="col-md-6 project-list">
    {{#each posts as |post|}}

    {{> post-info post }}

    {{!-- <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.title}}</a></h4>
      </div>
    </div> --}}
    
    {{/each}}
  </div>
  {{/if}}
</div>

</div>